apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ecvi-backend-metrics
  namespace: ecvi
  labels:
    app: backend
spec:
  selector:
    matchLabels:
      app: backend
  endpoints:
  - port: 8000
    path: /api/v1/metrics
    interval: 30s
    scrapeTimeout: 10s

